$kind: http-request
url: "{{baseUrl}}/api/v1/meetings/{{meetingUuid}}/notes/{{noteId}}"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "note": "Updated note text."
    }
order: 7000
