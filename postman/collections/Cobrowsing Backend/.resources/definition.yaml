$kind: collection
description: "Express + Socket.IO API server (EnableX, scheduling, analytics). Default base URL: http://localhost:5001"
variables:
  baseUrl: http://localhost:5001
  token: ""
  meetingUuid: ""
  noteId: ""
  scheduledId: ""
auth:
  - id: c9191dc2-07ac-4fcf-a094-3a54460406d2
    type: bearer
    name: bearer auth
    credentials:
      token: "{{token}}"
